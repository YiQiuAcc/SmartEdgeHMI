## 🗺️ 开发 TODO 事项 (Milestones)

### 阶段一：基础设施与架构搭建 (Day 1) [已完成]

- [x] **初始化仓库** ：创建 `.NET 8 WPF` 项目，配置 `.gitignore`。
- [x] **引入核心 NuGet 包** ：
    - `CommunityToolkit.Mvvm`、`ScottPlot.WPF`、`Serilog`、`Dapper` 等。
- [x] **搭建 MVVM 骨架** ：划分 Constants, Models, Services, ViewModels, Views 目录。
- [x] **工业级安全策略配置** ：
    - 利用 `Mutex` 实现全局单例进程锁，防止工控机重复启动导致硬件端口冲突。
    - 接管全局未处理异常（UI线程/后台线程/Task线程三级捕获），保障系统不盲目闪退。

### 阶段二：底层通信驱动层 (Day 2)

- [x] **定义通信契约** ：在 `Models` 中定义 `DeviceTelemetry`（遥测数据）和 `DeviceCommand`（下发指令）。
- [x] **编写 SerialPortService** ：
    - 封装扫描本机可用 COM 口、安全打开/关闭串口的方法。
    - **核心** ：开启后台长时间运行的 `Task` 监听串口，通过 `WeakReferenceMessenger` 异步广播 JSON 数据。
- [x] **实现双向控制** ：实现 `SendCommandAsync`，将 `DeviceCommand` 对象序列化为字节流写入串口。
- [x] **🚀【高级进阶提示】面向未来的工业标准驱动升级**：
    - _提示_：基础版跑通后，可扩展一个 `ModbusService`，手写 **CRC16 校验算法**，实现对标准 PLC（西门子/汇川等）的寄存器（Holding Registers）读写，展现“字节级操纵底层”的能力。
    - _提示_：利用 .NET 8 的 `ReadOnlySpan<byte>` 与内存池 `ArrayPool` 处理字节流，解决高频高并发下的**粘包断包**问题，主打“零分配内存（Zero-Allocation）与极低 GC 压力”。

### 阶段三：UI 构建与数据绑定 (Day 3-4)

- [x] **主控面板 (Dashboard)** ：
    - 顶部：连接状态指示灯（红/绿）、COM 口下拉框、波特率选择、连接/断开按钮。
    - 中间：动态曲线图 (使用 `ScottPlot.WpfPlot`)，实时绘制传感器数据（如温度趋势线）。
    - 底部：最新遥测数据的实时文本展示（类似汽车仪表盘）。
- [x] **控制下发面板 (Control Panel)** ：
    - 提供滑块 (Slider) 或输入框修改“温度报警阈值”。
    - 提供“设备复位 (Reset)” 紧急按钮（下发 `DeviceAction.Reset` 动作）。
- [x] **日志/报警视图 (Log & Alarm View)** ：
    - 使用 `DataGrid` 绑定本地 SQLite 的查询结果，清晰展示历史报警记录与异常日志。
- [ ] **🚀【高级进阶提示】面向大规模工业监控的 UI 调优**：
    - _提示_：针对 `DataGrid` 加载百万级历史数据联动图表时，引入 **LTTB（最大三角形三桶）数据降采样算法**，将百万点压缩至数千点交由 `ScottPlot` 的 OpenGL 模式渲染，实现历史趋势“秒级无卡顿加载”。
    - _提示_：升级报警模块，引入 **ISA-18.2 国际通用工业报警状态机**（未确认未恢复、已确认未恢复等），配合 UI 触发高优先级红光闪烁及一键“确认全选（Ack All）”。

### 阶段四：虚拟设备模拟器联调 (Day 4)

- [x] **编写虚拟下位机模拟脚本** ：
    - 使用 Python 或 Node.js 连接到虚拟串口（如经 VSPD 虚拟出的 COM2 口）。
    - 每隔 500ms 持续推送符合 `DeviceTelemetry` 格式的带随机波动的 JSON 报文。
    - 实时监听串口输入的控制指令，并在收到 WPF 的“修改阈值”或“设备复位”指令时做出状态改变。
- [x] **全链路打通测试** ：WPF 连接 COM1，模拟器连接 COM2，验证“接收采集-波形渲染-点击按钮下发-下位机响应”的闭环。

### 阶段五：边缘存储落地 (Day 5)

- [x] **封装 SQLite 服务** ：使用高性能 ORM 框架 `Dapper` 编写 `SqliteRepository`。
- [x] **基础触发逻辑** ：当接收到的底层设备发送错误代码来触发报警时，触发 UI 红色报警闪烁，同时将报警时间、当前值、阈值、QualityCode 等数据一并 `Insert` 进 SQLite 中，供 DataGrid 视图检索。
- [ ] **🚀【高级进阶提示】工业大数据高频持续写入性能优化**：
    - _提示_：引入高性能异步队列 `System.Threading.Channels`（生产者-消费者架构），采集到的高频数据不逐条写入，而是当队列满 500 条或到达 1 秒窗口时，触发一次 **Bulk Insert（批量合并写入）**，极大程度保护工控机固态硬盘寿命。
    - _提示_：显式配置 SQLite 为 **WAL（Write-Ahead Logging）预写日志模式** 调优，实现读写分离与高并发。

### 阶段六：真实硬件接入 (进阶彩蛋)

- [ ] 掏出 Arduino / ESP32 硬件，编写 C++ 固件利用 `Serial.println()` 向 USB 发送与模拟器格式一致的报文。
- [ ] 解析来自 WPF 的重置或动作指令，控制硬件板载 LED 闪烁或继电器跳闸，录制完美的简历演示视频（演示物理硬件与软件的数字孪生交互）。

### 阶段七：工控核心——稳定性与工程化 (🚀 面试必杀技)

- [ ] **软件生命周期自守护（Watchdog 守护进程）**：
    - 编写一个控制台小软件作为外部看门狗，通过命名管道（Named Pipe）与 WPF 保持心跳。若主程序由于第三方驱动死锁，看门狗在 3 秒内强行重启 HMI 并恢复先前运行状态。
- [ ] **工厂多语言国际化（i18n）**：
    - 基于 WPF 资源字典（ResourceDictionary）实现运行期**中英文一键无缝切换**，满足设备出口和外企工厂规范。
- [ ] **核心驱动层单元测试（Unit Testing）**：
    - 使用 `XUnit` 对通信解包、CRC校验和报警状态机编写单元测试，展现高代码质量控制标准。
